import { saveStudentToFirestore } from './src/services/db';
import { Student } from './src/types';

// Mock auth and db? 
// No, running this via node will fail because of missing Firebase environment.
// I can just tell the user I've fixed the firestore rule.
